spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.utaboakoxfaczrfylszg.supabase.co:5432/postgres?user=postgres&password=OSKOgroupeprojectbd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

debug: true

server:
  port: 8086
